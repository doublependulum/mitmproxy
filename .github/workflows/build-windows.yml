name: Build Windows Executable

on:
  push:
    branches:
      - '**'
      - '!dependabot/**'
      - '!*-patch-*'
  pull_request:
  workflow_dispatch:

jobs:
  build-windows-executable:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version:  .github/python-version.txt  # specify the Python version you need
      - run: pip install -e .[dev]  # pyinstaller 5.9 does not like pyproject.toml + editable installs.
      - run: python -u release/build.py installbuilder-installer msix-installer

      - uses: actions/upload-artifact@v4
        with:
          name: binaries.windows-installer
          path: release/dist
